#include <stdio.h>

int main() {
    int n = 4;
    if (n % 2 == 0) {
        printf("%d is Even", n);        
    }
    if (n % 2 != 0) {
        printf("%d is Odd", n);        
    }

    return 0;
}
