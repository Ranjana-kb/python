 num = int(input("Enter an integer: "))
start = 10
end = 20
for i in range(start, end + 1):
    if num == i:
        print(f"{num} lies in the range from {start} to {end}.")
        break
else:
    print(f"{num} does not lie in the range from {start} to {end}.")
